{
  "name": "@tenvra/coordinator",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx watch src/server.ts",
    "lint": "eslint .",
    "start": "node dist/server.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tenvra/contracts": "workspace:*",
    "fastify": "^5.6.0"
  },
  "devDependencies": {
    "@tenvra/eslint-config": "workspace:*",
    "@tenvra/typescript-config": "workspace:*",
    "@types/node": "^25.9.3",
    "eslint": "^9.34.0",
    "tsx": "^4.20.5",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
