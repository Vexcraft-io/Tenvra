name: DCO

on:
  pull_request:

permissions:
  contents: read
  pull-requests: read

jobs:
  dco:
    runs-on: ubuntu-latest
    steps:
      - uses: christophebedard/dco-check@0.5.1
        with:
          python-version: "3.12"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
